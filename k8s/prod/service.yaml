# k8s/prod/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: incentivize-this-frontend-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: incentivize-this-frontend